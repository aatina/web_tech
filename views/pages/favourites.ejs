<!DOCTYPE html>
<html>

	<head>
    <% include ../partials/head %>
	</head>

  <body>

		<header>
			<% include ../partials/header %>
		</header>

		<div class="content-wrapper container">
			<h2>Favourites</h2>
			 <% if (locals.sess_user) { %>
				 <% include ../partials/recipes_view %>
			 <% } else { %>
				 <p> Please <a href = "/login">log in</a> to view your favourites! </p>
			 <% } %>
		</div>
		
		<% include ../partials/chatbox %>

		<footer>
			<% include ../partials/footer %>
		</footer>

  </body>

</html>
